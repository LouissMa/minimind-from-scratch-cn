# 环境搭建记录

> 记录我在本地搭建 MiniMind 训练环境的真实过程，方便自己和他人复现。
> 建议边搭边填，把**真实报错和解决办法**记下来——这些才是复现价值所在。

---

## 硬件

| 项 | 值 |
|----|-----|
| GPU | *（待填，如 RTX 3060 12G）* |
| 显存 | |
| 内存 | |

## 软件版本

| 项 | 值 |
|----|-----|
| 操作系统 | Windows 11 |
| Python | *（待填）* |
| CUDA / 驱动 | |
| PyTorch | 2.6 |

---

## 安装步骤

### 1. 创建虚拟环境
```bash
python -m venv .venv
.venv\Scripts\activate      # Windows
```

### 2. 安装 PyTorch（按你的 CUDA 版本选择）
```bash
# 示例（CUDA 12.x），实际以 https://pytorch.org 为准
pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu124
```
> ⚠️ 记录：我实际用的安装命令是 ______，为什么这么选：______

### 3. 安装其余依赖
```bash
pip install -r requirements.txt
```

### 4. 验证 GPU 可用
```bash
python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'CPU')"
```

---

## 踩坑记录

> 把安装 / 首次运行时遇到的报错和解决办法记在这里。示例：

- **问题**：
  - **报错信息**：
  - **原因**：
  - **解决**：

---

## 数据集下载

- 数据集较大，需自行下载放入 `dataset/`（已在 `.gitignore` 排除，不进仓库）。
- 下载来源见 [dataset/dataset.md](../dataset/dataset.md)（原项目提供 ModelScope / HuggingFace 链接）。
- 我实际下载了哪些文件、各多大：
  | 文件 | 用途 | 大小 |
  |------|------|------|
  | pretrain_hq.jsonl | 预训练 | |
  | sft_mini_512.jsonl | SFT | |
